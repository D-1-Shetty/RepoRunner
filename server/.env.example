PORT=
MONGO_URI=
JWT_SECRET=

# Base64-encoded 32-byte key used to encrypt stored deployment environment
# values (AES-256-GCM). Generate one with:
#   node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
CONFIG_ENCRYPTION_KEY=
